<style>
  @import url("https://fonts.googleapis.com/css2?family=Birthstone&display=swap");
</style>

<nav class="fixed top-0 left-0 w-full z-50 bg-[#E4E0E1] shadow-md">
  <div class="container mx-auto flex flex-wrap items-center justify-around p-4">
    <a href="#home" class="text-[40px] font-bold font-['Birthstone']">Faprin</a>

    <div class="hidden md:flex items-center gap-6 lg:gap-14">
      <a
        href="#home"
        class="hover:scale-110 transform transition duration-200 cursor-pointer ease-in-out before:transition-[width] before:ease-in-out before:duration-700 before:absolute before:bg-gray-400 before:origin-center before:h-[1px] before:w-0 hover:before:w-[50%] before:bottom-0 before:left-[50%] after:transition-[width] after:ease-in-out after:duration-700 after:absolute after:bg-gray-400 after:origin-center after:h-[1px] after:w-0 hover:after:w-[50%] after:bottom-0 after:right-[50%]"
        >Inicio</a
      >
      <a
        href="#about "
        class="hover:scale-110 transform transition duration-200 cursor-pointer ease-in-out before:transition-[width] before:ease-in-out before:duration-700 before:absolute before:bg-gray-400 before:origin-center before:h-[1px] before:w-0 hover:before:w-[50%] before:bottom-0 before:left-[50%] after:transition-[width] after:ease-in-out after:duration-700 after:absolute after:bg-gray-400 after:origin-center after:h-[1px] after:w-0 hover:after:w-[50%] after:bottom-0 after:right-[50%]"
        >¿Quién soy?</a
      >
      <a
        href="#projects"
        class="hover:scale-110 transform transition duration-200 cursor-pointer ease-in-out before:transition-[width] before:ease-in-out before:duration-700 before:absolute before:bg-gray-400 before:origin-center before:h-[1px] before:w-0 hover:before:w-[50%] before:bottom-0 before:left-[50%] after:transition-[width] after:ease-in-out after:duration-700 after:absolute after:bg-gray-400 after:origin-center after:h-[1px] after:w-0 hover:after:w-[50%] after:bottom-0 after:right-[50%]"
        >Proyectos</a
      >
      <a
        href="#contact"
        class="hover:scale-110 transform transition duration-200 cursor-pointer ease-in-out before:transition-[width] before:ease-in-out before:duration-700 before:absolute before:bg-gray-400 before:origin-center before:h-[1px] before:w-0 hover:before:w-[50%] before:bottom-0 before:left-[50%] after:transition-[width] after:ease-in-out after:duration-700 after:absolute after:bg-gray-400 after:origin-center after:h-[1px] after:w-0 hover:after:w-[50%] after:bottom-0 after:right-[50%]"
        >Contacto</a
      >
    </div>
  </div>
</nav>

<main class="max-w-[90%] md:max-w-[60%] mx-auto">
  <section
    id="home"
    class="h-screen flex flex-col md:flex-row justify-center items-center text-center md:text-left"
  >
    <!-- -- Texto reactivo ---->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 h-auto md:h-[240px]">
      <div
        class="h-full min-h-[240px] flex flex-col md:text-end md:flex-row justify-center md:justify-end gap-4 md:gap-10"
      >
        <h3 id="nombre" class="text-4xl md:text-6xl font-bold">
          Jose Manuel<br /> Domínguez <br /> Segura
        </h3>
        <div class="bg-[#493628] w-[0px] md:w-1 h-full"></div>
      </div>

      <div>
        <div
          class="flex flex-col md:flex-row justify-between items-center text-xl md:text-3xl"
        >
          <div class="flex gap-2 items-center">
            <h4 class="font-bold italic">¡Hola!</h4>
            <span class="icon-[line-md--emoji-smile-wink]"></span>
          </div>

          <div class="flex gap-2 items-center mt-3 md:mt-0">
            <a
              href="https://github.com/Faprin"
              target="_blank"
              area-label="Visitar el perfil de GitHub de Faprin"
              rel="noopener noreferrer"
            >
              <span class="icon-[line-md--github-loop]"></span>
            </a>
            <a
              href="https://www.linkedin.com/in/jose-manuel-dom%C3%ADnguez-segura-9b779b312/"
              target="_blank"
              area-label="Visita mi perfil de LinkedLin de Jose Manuel Domínguez Segura"
              rel="noopener noreferrer"
            >
              <span class="icon-[line-md--linkedin]"></span>
            </a>
          </div>
        </div>
        <p class="text-sm md:text-base mt-4">
          ¡Bienvenido a mi portafolio! Soy estudiante de Ingeniería Informática
          en la Universidad de Huelva, especializado en arquitectura del
          software. Aquí comparto mis proyectos personales y algunos académicos,
          los cuales iré completando y actualizando conforme avance en mi
          formación. Te invito a explorar mis trabajos y conocer más sobre mis
          habilidades en el diseño y optimización de sistemas.
        </p>
      </div>
    </div>
  </section>

  <section id="about" class="min-h-screen mb-[70px]">
    <div class="pt-20 md:pt-36">
      <h3 class="text-lg md:text-md">¿Quién soy?</h3>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-10">
        <div class="flex flex-col gap-5">
          <p class="text-3xl md:text-5xl font-bold">
            Código, innovación y eficiencia: Construyendo el software del
            futuro.
          </p>
          <p class="text-sm md:text-base">
            El desarrollo de software es más que escribir código; es crear
            soluciones que realmente marquen la diferencia. Me especializo en
            diseñar software eficiente, seguro y escalable, con un enfoque
            particular en la optimización de procesos y la programación
            concurrente. Me apasiona la algoritmica, ya que permite resolver
            problemas complejos de manera elegante, y disfruto enormemente del
            desarrollo backend, donde puedo crear soluciones robustas y
            eficientes. A lo largo de mi formación en Ingeniería Informática,
            sigo perfeccionando mis habilidades y aprendiendo nuevas técnicas
            que me permitan enfrentar cualquier desafío que el desarrollo de
            software me proponga.
          </p>
          <button
            class="rounded-full py-2 px-6 border-[#493628] border-2 max-w-[140px] hover:scale-110 hover:bg-[#493628] hover:text-white transition"
          >
            <a href="#contact">Contáctame</a>
          </button>
        </div>
        <div class="flex justify-center md:justify-end">
          <img src="/mifiot.png" alt="Jose Manuel Domínguez Segura" style="width: 100%; height: auto;">
        </div>
      </div>
    </div>
  </section>

  <section id="projects" class="min-h-screen mb-[70px]">
    <div class="text-center pt-20 md:pt-36">
      <h3 class="font-bold text-3xl md:text-4xl">Proyectos</h3>
      <p class="text-sm md:text-base">
        *Todos los proyectos se encuentran en GitHub
      </p>
    </div>

    <div
      id="project-cont"
      class="flex flex-col gap-10 pt-7 transition-all duration-1000"
    >
    </div>
  </section>
  <section
    id="contact"
    class="min-h-screen pt-40 mb-[110px] md:mb-[0px] md:flex md:pt-[200px]"
  >
    <div class="grid grid-cols-1 md:grid-cols-2 gap-10">
      <div class="flex justify-center md:justify-start">
        <img
          src="/3.png"
          alt="dev"
          class="w-[80%] md:pt-5 md: md:w-full h-[300px] md:h-[500px] object-contain"
        />
      </div>

      <div class="flex flex-col gap-4">
        <h3 class="font-bold text-3xl md:text-6xl">Contacto</h3>
        <p class="text-sm md:text-base">
          Para cualquier consulta o colaboración, no dudes en ponerte en
          contacto conmigo.
        </p>
        <form
          class="flex flex-col gap-4"
          action="mailto:jmdominguezsegura@gmail.com"
          method="post"
          enctype="text/plain"
        >
          <input
            class="bg-transparent border-[#493628] border-2 rounded-full px-4 py-2 focus:shadow-lg transition"
            type="text"
            name="mail"
            placeholder="Correo"
          />
          <input
            class="bg-transparent border-[#493628] border-2 rounded-full px-4 py-2 focus:shadow-lg transition"
            type="text"
            name="name"
            placeholder="Nombre"
          />
          <textarea
            class="bg-transparent border-[#493628] border-2 rounded-3xl px-4 py-2 h-24 resize-none focus:shadow-lg transition"
            placeholder="Mensaje"
            name="comment"></textarea>
          <input
            type="submit"
            value="Enviar"
            class="rounded-full px-6 py-2 border-[#493628] border-2 hover:bg-[#493628] hover:text-white transition"
          />
        </form>
      </div>
    </div>
  </section>
</main>

<script>
  const projectsTitle = [
    "N-Queens Solver",
    "Problema del viajante (Práctica Universitaria)",
  ];
  const projectsDescriptions = [
    "Este proyecto resuelve el problema de las N-Reinas utilizando una interfaz gráfica construida con JavaFX. El programa permite al usuario introducir el número de filas, columnas y reinas a través de la interfaz, y genera las posibles soluciones en un fichero utilizando el algoritmo de backtracking.",
    "Programa en lenguaje Java con el que podamos resolver el famoso problema del viajante. Para ello, empleamos distintas estrategias algorítmicas y realizamos un estudio de cada uno de los algoritmos usados, verificando cuál resulta más eficiente y gasta menos tiempo computacional.",
  ];
  const projectsLinks = [
    "https://github.com/Faprin/n-Queens",
    "https://github.com/Faprin/Practica2_AMC",
  ];
  const projectsImages = ["/tablero.webp", "/viajante.webp"];
  const altImg = ["tablero", "viajante"];

  function generateProjects() {
    const content = document.getElementById("project-cont");
    if (content != null) content.innerHTML = "";

    projectsTitle.forEach((title, index) => {
      const isEven = index % 2 === 0;

      const projectHTML = `
      <div class="grid grid-cols-1 md:grid-cols-2 md:mx-20 opacity-0 translate-y-10 transition-all duration-700">
        ${
          isEven
            ? `<div id="column-1" class="flex flex-col ml-auto text-end pr-10">
              <a class="" href="${projectsLinks[index]}" target="_blank" rel="noopener noreferrer">
                <h3 class="font-bold text-2xl hover:scale-105 transform transition duration-200 ease-in-out">${title}</h3>
              </a>
              <p>${projectsDescriptions[index]}</p>
            </div>
            <div id="column-2" class="flex">
              <div class="bg-[#493628] max-md:hidden md:w-1 md:max-w-[3px] h-full mr-10"></div>
              <img class="w-full max-w-[0px] md:max-w-[210px] object-contain" src="${projectsImages[index]}" alt="${altImg[index]}" />
            </div>`
            : `<div id="column-1" class="flex justify-end mr-10">
              <img class="w-full max-w-[0px] md:max-w-[210px] object-contain" src="${projectsImages[index]}" alt="${altImg[index]}" />
            </div>
            <div class="flex">
              <div class="bg-[#493628] max-md:hidden md:w-1 md:max-w-[3px] w-1 h-full mr-10"></div>
              <div id="column-2" class="flex flex-col text-start">
                <a href="${projectsLinks[index]}" target="_blank" rel="noopener noreferrer">
                  <h3 class="font-bold text-2xl hover:scale-105 transform transition duration-200 ease-in-out">${title}</h3>
                </a>  
                <p>${projectsDescriptions[index]}</p>
              </div>
            </div>`
        }
      </div>`;
      if (content != null) content.innerHTML += projectHTML;
    });

    applyIntersectionObserver();
  }

  function applyIntersectionObserver() {
    const projectItems = document.querySelectorAll("#project-cont > div"); // Selecciona cada proyecto

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry, index) => {
          if (entry.isIntersecting) {
            setTimeout(() => {
              entry.target.classList.add("opacity-100", "translate-y-0");
            }, index * 200);
            observer.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.2 }
    );

    projectItems.forEach((item) => observer.observe(item));
  }

  document.addEventListener("DOMContentLoaded", () => {
    generateProjects();
  });

  const nombre = document.getElementById("nombre");

  const nombres = [
    "Jose Manuel Domínguez Segura",
    "Ingeniero Informático",
    "Arquitecto de Software",
    "Desarrollador Full Stack",
  ];

  let index = 0;

  function changeName() {
    if (nombre != null) {
      nombre.innerHTML = "";

      const currentName = nombres[index];
      const words = currentName.split(" ");

      let letterIndex = 0;
      let wordIndex = 0;

      function printLetter() {
        if (nombre != null) {
          // mientras que no haya terminado una palabra, imprimo sus letras
          if (letterIndex < words[wordIndex].length) {
            // mientras que la letra que quiero escribir sea menor al largo de la palabra
            nombre.innerHTML += words[wordIndex][letterIndex]; // imprimo la letra
            letterIndex++; // aumento la letra
            setTimeout(printLetter, 100);
          } else {
            // añado un salto de linea
            nombre.innerHTML += "<br>";

            wordIndex++;
            letterIndex = 0;

            if (wordIndex < words.length) {
              setTimeout(printLetter, 100);
            } else {
              // acabo una palabra
              index = (index + 1) % nombres.length;
              setTimeout(changeName, 1500);
            }
          }
        }
      }
      printLetter();
    }
  }

  changeName();
</script>
